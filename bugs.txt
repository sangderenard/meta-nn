check the IR+mermaid+runtime system to see if the project has drifted and the system of IR based coordination and representation needs updating
system instability (System service exception, hunch: stale GPU allocation accessed lazily after race condition)
edges not showing up as target or found, can't see it in mask as edge highlighting either
history needs scaling/normalizing/range controls
saving doesn't seem to be working, or else restoring, possible blank initial save still
checkpoints need to be selectable in the gui, maybe next and prev buttons by the scrub wheel
targets are still a truncated list going horizontal now a list and not separate the stats
move step loss and batch loss into the controls panel under the under the live/history placement of the scrub indicator
check if the target on ly detected only are true and properly accurate or simple subtraction of area or something - can we get better mask stats
overloading (loading more rows than are planned to be used)
eager loading (loading before any gate would allow use)
underutilization of gpu in the faux model adapter processes
microbatch cap too sensitive
graph display on gui needs reworking
figure out the right panel and how to show a diagnostic image of the weights as it changes over time, consider shopping it out to the gui gpu, aren't they using different devices? maybe it's a bad plan, but still the last checkpoint weights isn't a bad idea
don't redraw the gui if there's nothing new
if evals aren't on their own thread, they need to use the gpu management system, if evals are on their own thread, they're not doing their job. priority is expressibility through the IR+mermaid+runtime
"[data-node] _flatten_symbol_pool: 38 pool terms not in vocab: ['digit 0', 'digit 1', 'digit 2', 'digit 3', 'digit 4', 'digit 5', 'digit 6', 'digit 7', 'digit 8', 'digit 9', 'emnist dataset', 'letter a', 'letter b', 'letter c', 'letter d', 'letter e', 'letter f', 'letter g', 'letter h', 'letter i', 'letter j', 'letter k', 'letter l', 'letter m', 'letter n', 'letter o', 'letter p', 'letter q', 'letter r', 'letter s', 'letter t', 'letter u', 'letter v', 'letter w', 'letter x', 'letter y', 'letter z', 'mnist dataset']" this should not be happening, the datasets I know for a fact are supposed to be in the basic 151 hot always on vocab. churn should enable these, sure, but I was certain the datasets were actually in the basic vocabulary and honestly I thought the digits were too. investigate BEFORE changes
